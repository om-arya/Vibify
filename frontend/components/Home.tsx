import React from 'react';
import { StyleSheet, Text, View, ScrollView } from 'react-native';

const Home: React.FC = () => {
    return (
        <ScrollView>
            <Text>Hello!</Text>
        </ScrollView>
    )
}

export default Home;